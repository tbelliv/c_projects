#include<stdio.h>
#include<math.h>

int main(){

    double a = sqrt(9);
    double b = pow(2,4);
    int c = round(3.14);
    int d = floor(3.99);
    double f = fabs(-100);
    double g = log(3);
    double h = sin(45);
    double i = cos(45);
    double j = tan(45);

    printf("\n%lf\n\n", h);

    return 0;
}